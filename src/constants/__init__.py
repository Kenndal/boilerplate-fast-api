# Database schema
API_SCHEMA_NAME = "boilerplate_api"

# API VERSION Prefix
VERSION_PREFIX = "v1"

# API Prefix
USER_PREFIX = "users"

# Query params
PAGE_NUMBER = "pageNumber"
PAGE_SIZE = "pageSize"
OMIT_PAGINATION = "omitPagination"
SORT_BY = "sortBy"
SORT_DIRECTION = "sortDirection"

# Default values
DEFAULT_PAGE_NUMBER = 1
DEFAULT_PAGE_SIZE = 10
